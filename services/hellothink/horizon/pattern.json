{
    "name": "pattern-${SERVICE_NAME}-$ARCH",
    "label": "Think Moscow services pattern for $ARCH",
    "description": "Think Moscow services pattern for $ARCH",
    "public": false,
    "services": [
        {
            "serviceUrl": "$SERVICE_NAME",
            "serviceOrgid": "$HZN_ORG_ID",
            "serviceArch": "$ARCH",
            "serviceVersions": [
                {
                    "version": "$SERVICE_VERSION"
                }
            ]
        }
    ]
}