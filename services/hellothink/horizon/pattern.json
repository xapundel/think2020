{
    "name": "pattern-${SERVICE_NAME}-$ARCH",
    "label": "Think Moscow services pattern for $ARCH -- USER=${HORIZON_USER}",
    "description": "Think Moscow services pattern for $ARCH -- USER=${HORIZON_USER}",
    "public": false,
    "services": [
        {
            "serviceUrl": "$SERVICE_NAME",
            "serviceOrgid": "$HZN_ORG_ID",
            "serviceArch": "$ARCH",
            "serviceVersions": [
                {
                    "version": "$SERVICE_VERSION"
                }
            ]
        }
    ]
}