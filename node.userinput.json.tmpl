{
  "services": [
    {
      "org": "${HZN_ORG_ID}",
      "url": "${SERVICE_NAME}",
      "versionRange": "[0.0.0,INFINITY)",
      "variables": {
        "MQTT_BROKER_URI": "${MQTT_BROKER_URI}"
      }
    }
  ]
}
